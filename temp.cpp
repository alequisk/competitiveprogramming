#include<bits/stdc++.h>
using namespace std;

typedef int64_t ll;
const char nl = '\n';
#define len(x) (int)(x).size()
#define all(x) (x).begin(), (x).end()

void solve() {
}

int main() {
  cin.tie(0)->sync_with_stdio(0);
  int testcase;
  cin >> testcase;

  for (int ncase = 1; ncase <= testcase; ++ncase) {
    solve();
  }
  
  return 0;
}